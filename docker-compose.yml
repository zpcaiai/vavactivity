name: vav-platform

include:
  # Treat base + development as one included application model. Listing them
  # as separate includes makes Compose reject intentional service overrides.
  - path:
      - deploy/compose/docker-compose.base.yml
      - deploy/compose/docker-compose.dev.yml
