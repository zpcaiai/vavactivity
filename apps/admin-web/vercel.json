{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "buildCommand": "pnpm --dir ../user-web exec vue-tsc -b && pnpm --dir ../user-web exec vite build --outDir ../admin-web/dist --emptyOutDir",
  "outputDirectory": "dist",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
