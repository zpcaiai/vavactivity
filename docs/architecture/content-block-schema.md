# Structured content blocks

The supported version-one block registry includes hero, rich text, image, quote, call to
action, feature/list collections, FAQ and divider blocks. Each block carries a stable ID,
type, schema version and typed data object.

Rich text is controlled editor JSON. Script, iframe, style, form, object, embed, event-handler
markup and `javascript:` links are rejected before persistence.
