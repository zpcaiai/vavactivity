# Administration field masking standard

Restricted fields are masked by backend DTOs. Reveal grants bind one administrator, purpose, policy and entity; require a reason and current step-up authentication; expire automatically; and are audited.
