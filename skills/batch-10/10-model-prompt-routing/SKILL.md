---
name: vav-model-prompt-routing
description: Version prompts and route typed tasks through compatible primary/fallback providers with budgets.
---

Require structured/tool capabilities by task, immutable approved releases, evaluation before activation, rollback,
provider health/budget checks and token/latency/cost traces. Production must reject fake providers.
