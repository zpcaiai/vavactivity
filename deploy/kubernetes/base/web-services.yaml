apiVersion: v1
kind: Service
metadata: {name: user-web}
spec: {selector: {app.kubernetes.io/name: user-web}, ports: [{name: http, port: 80, targetPort: http}]}
---
apiVersion: v1
kind: Service
metadata: {name: admin-web}
spec: {selector: {app.kubernetes.io/name: admin-web}, ports: [{name: http, port: 80, targetPort: http}]}
