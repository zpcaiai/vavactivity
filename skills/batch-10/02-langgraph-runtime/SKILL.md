---
name: vav-langgraph-runtime
description: Build the explicit versioned Hanna LangGraph and durable application checkpoints.
---

Use a typed `StateGraph`, explicit conditional edges, bounded retries/timeouts and redacted node traces.
Persist encrypted checkpoints after nodes; resume only compatible graph/state versions and isolate side effects.
