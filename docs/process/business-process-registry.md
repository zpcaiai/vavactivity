# Business process registry

Batch 24 registers 16 critical cross-domain processes in `config/process/business-processes.yaml`. Definitions are versioned, checksum-bound and declare owners, participants, actors, terminal outcomes, SLA, cancellation, compensation and stuck policies. Domain modules remain authoritative.
