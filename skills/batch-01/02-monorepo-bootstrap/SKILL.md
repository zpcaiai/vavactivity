---
name: vav-monorepo-bootstrap
description: Create the reproducible Python and pnpm workspace.
---

Maintain separate user, admin, API and worker applications, shared packages, infrastructure, documentation and scripts. Pin dependencies in `uv.lock` and `pnpm-lock.yaml`; keep all commands macOS and Linux compatible.

