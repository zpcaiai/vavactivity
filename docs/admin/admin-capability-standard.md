# Administration capability standard

Every write capability binds a stable code to one owning module, permission, risk, audit level and registered domain command. The administration platform never exposes arbitrary SQL or direct state mutation.
