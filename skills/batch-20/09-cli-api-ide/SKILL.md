---
name: vav-skill-cli-api-ide
description: Implement Skill lifecycle CLI commands, registry/install/execution APIs, diagnostics, schema completion, local debug, trace viewing, publishing, or Codex/IDE integration boundaries.
---

Keep CLI and API behavior on the same application services. Provide dry-run and machine-readable output, safe errors, explicit version selection, scoped auth, and audit identity. IDE helpers validate locally but cannot bypass server signing, permission, review, or publication gates.
