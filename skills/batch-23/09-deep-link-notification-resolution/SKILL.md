---
name: vav-experience-deep-link-notification-resolution
description: Resolve scoped user-bound deep links and notification actions with expiry, replay prevention and safe fallbacks.
---

Hash tokens at rest, enforce purpose/user/entity/expiry and consume single-use links atomically. Recheck permissions and safety on resolution. Changed or missing resources must return a registered fallback, not sensitive errors.
