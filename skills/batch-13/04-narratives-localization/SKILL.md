# Multilingual narratives

Store one narrative row per locale with length limits from configuration. Contact details and
external links are rejected outright so members cannot bypass the consent-gated contact exchange;
scam, payment and off-platform patterns route to human review instead of a silent verdict. AI may
polish wording, but the member must confirm the result before it is stored as their own statement,
and the platform never fabricates credentials or faith histories.
