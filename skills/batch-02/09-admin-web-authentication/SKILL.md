---
name: vav-admin-web-authentication
description: Build isolated administrator login, invitation and permission-aware navigation.
---

Use the admin JWT audience and admin refresh cookie. Remove preview bypasses, enforce
backend permissions for every API and hide unavailable navigation only as a UX aid,
never as authorization.
