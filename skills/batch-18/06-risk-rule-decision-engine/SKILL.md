---
name: vav-risk-rule-decision-engine
description: Evaluate versioned registered-signal safety rules with deterministic explanations and rollback.
---

Allow only the registered-signal DSL operators `eq`, `gte`, `lte` and `in`. Never evaluate code or
SQL. Apply blocks and active restrictions before model/rate signals. Require another administrator to
activate high-impact rules and retain every immutable risk decision with policy version and reason code.
