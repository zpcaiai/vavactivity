---
name: vav-one-command-acceptance
description: Prove a new developer can start and verify the platform.
---

`scripts/verify.sh` must validate Compose, rebuild the stack, wait for health, probe API and web applications, migrate a clean database, run backend and frontend tests, and emit a concise acceptance result.

