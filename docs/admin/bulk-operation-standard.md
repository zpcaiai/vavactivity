# Bulk operation standard

Selections are immutable snapshots. Dry Run, per-item eligibility, state revalidation, idempotency, registered domain commands, partial results and safe failed-item retries are mandatory.
