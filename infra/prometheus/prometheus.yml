global: {scrape_interval: 15s, evaluation_interval: 15s}
rule_files: [/etc/prometheus/platform.rules.yml]
scrape_configs:
  - job_name: vav-api
    metrics_path: /api/v1/health/metrics
    static_configs: [{targets: [api:8000]}]
  - job_name: prometheus
    static_configs: [{targets: [localhost:9090]}]
