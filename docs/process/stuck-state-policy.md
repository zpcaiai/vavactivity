# Stuck-state policy

The scanner detects process and progress SLA breaches, creates one deduplicated finding and an intervention task, and permits only registered retry or projection-rebuild commands. Direct SQL, direct state edits and fabricated outcomes are rejected and audited.
