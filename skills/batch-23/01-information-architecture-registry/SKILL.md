---
name: vav-experience-information-architecture-registry
description: Validate and synchronize versioned VAV information-architecture spaces, nodes, localization and primary destinations.
---

Read `config/experience/information-architecture.yaml`, run `make experience-ia-check`, require unique nodes, valid parents, localized labels and one valid primary route per critical node. Activated IA versions are immutable; retire with migration guidance instead of editing history.
