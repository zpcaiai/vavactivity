---
name: vav-design-language-tokens
description: Build, validate, diff or release VAV primitive, semantic, component, layout, motion and density tokens.
---

Treat the manifest and category JSON files as sources. Generate every target deterministically, reject hard-coded colors in governed UI, preserve compatibility aliases, and require independent approval plus checksum-bound evidence before release.
