# In-app notifications

Implement owner-scoped list, detail, unread count, idempotent read, mark-all-read and archive.
Withdraw rather than delete evidence; expire unsafe actions; build deep links from a controlled
route registry only, with the database as the final unread-count source.
