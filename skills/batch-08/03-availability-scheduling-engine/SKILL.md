---
name: vav-availability-scheduling-engine
description: Generate timezone-safe counseling slots and atomic holds.
---
# Workflow
Generate from recurring rules, apply overrides, buffers and limits, then exclude
confirmed appointments and active holds. Lock final allocation in PostgreSQL.
