---
name: vav-project-charter
description: Keep project boundaries and unresolved business decisions machine-verifiable.
---

Validate `project-manifest.yaml` with `python scripts/validate_manifest.py`. Every module must have a phase, owner and status. Every pending decision must remain `undecided` with a null value until an accountable owner records approval.

