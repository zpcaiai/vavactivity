# Data ownership

`config/data/ownership.yaml` registers 24 canonical and derived assets. Each asset has one owning module, steward, classification, retention, erasure and canonical identifier policy. Source-of-truth and rebuildable projection roles are mutually exclusive.
